# Telegram-бот (устаревший, но стабильный API — sync-only)
python-telegram-bot==13.15

# Безопасность и конфигурация
python-dotenv>=0.19.0

# Логирование (лёгкий, удобный — как ты любишь)
loguru>=0.6.0

# Сетевые запросы (если нужно взаимодействие с внешними API)
requests>=2.25.0

# Опционально: БД (если нужна локальная БД — sqlite встроен)
# aiosqlite не ставится на 3.7.3 без async, но sqlite3 — да
# (используй built-in sqlite3 или добавь pympler для отладки памяти)

# Совместимость с твоим ML-стеком (только если нужна интеграция бота с моделью)
# ⚠️ Убери эти строки, если бот НЕ вызывает ML-модель напрямую:
numpy==1.21.6          # последняя, поддерживающая Python 3.7
pandas==1.3.5          # совместима с 3.7, CatBoost 1.1+
catboost==1.1.1        # последняя, поддерживающая 3.7
joblib>=1.0.0