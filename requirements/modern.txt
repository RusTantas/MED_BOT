# Telegram-бот (актуальный API — async, webhooks, type hints)
python-telegram-bot==20.7

# Безопасность и конфиг
python-dotenv>=1.0.0
python-decouple>=3.8   # альтернатива для строгой типизации конфигов

# Логирование + трассировка
loguru>=0.7.0
aiosqlite>=0.19.0      # async SQLite — для stateful-бота

# HTTP-клиенты (лучше, чем requests для async)
httpx>=0.24.0
aiofiles>=22.1.0       # async работа с файлами (если сохраняешь фото/документы)

# ML-стек (актуальные версии — совместимы с 3.9+)
numpy>=1.23.0
pandas>=1.5.0
catboost>=1.2.5
scikit-learn>=1.2.0
mlflow>=2.4.0

# Для отладки и профилирования (на твоих машинах)
psutil>=5.9.0
memory-profiler>=0.61.0